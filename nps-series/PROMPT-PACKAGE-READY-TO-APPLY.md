# Prompt Package — Ready-to-Apply Spec (awaiting 2 user inputs)

The package files (v2.1–v2.3) were lost in a workspace reset and were never committed anywhere.
This spec records everything still on record so that the moment the two missing inputs arrive,
the rebuild is a single mechanical turn.

## Task (user-defined, unchanged)

1. **Insert §17A–§17M verbatim** immediately after **§17 (Non-Repetition Engine)**.
2. **Replace ONLY the opening of §39** with the line recorded below — the remainder of §39 stays untouched.

## Content already on record (verbatim, user-supplied)

### §39 opening replacement — COMPLETE

> The model has NO authority to create law. Its sole function is to retrieve, verify, explain, visualize and test the law contained in authoritative sources.

### §17A–§17M — NOT on record

The user-supplied verbatim text was lost in a session context reset. It is private document
content (not recoverable from web, git, or any workspace location — exhaustively verified).
Re-share is mandatory.

## Placement & acceptance criteria (standing user constraints)

- New sections go **after §17 Non-Repetition Engine**, at the specified position.
- The §39 change is an **opening replacement only**; everything else in §39 is untouched.
- §17A–§17M must be transcribed **exactly as supplied** — no rewording, summarizing, or "improving."
- Zero legal invention: only user-supplied verbatim text enters the document.

## Required inputs (2)

1. **The base prompt document** — the one containing §17 Non-Repetition Engine and §39
   (paste into chat, or point to a GitHub repository it lives in).
2. **§17A–§17M verbatim** (paste into chat).

## Apply procedure (on receipt — one turn)

1. Save the base document as the package file.
2. Insert §17A–§17M after §17, character-exact (preserving the user's numbering/whitespace).
3. Replace the §39 opening with the line on record above.
4. Diff review — exactly two hunks, nothing else.
5. Deliver + archive to this branch.
